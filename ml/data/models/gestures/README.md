# Gesture model artifacts

This directory stores local MediaPipe landmark gesture models created by the
Phase 2 training scripts.

Generated files such as `model.joblib`, `scaler.joblib`, `labels.json`,
`metrics.json`, `metadata.json`, and `active_model.json` are intentionally
kept out of git.
